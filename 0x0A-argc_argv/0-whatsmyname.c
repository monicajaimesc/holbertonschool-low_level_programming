#include "holberton.h"
#include <stdio.h>
/**
 * main - program name, followed by a new line
 *
 *@argc: argument count
 *@argv: argument value
 * Return: zero
 */

int main(int __attribute__((unused)) argc, char *argv[])
{

	printf("%s\n", argv[0]);
	return (0);
}
